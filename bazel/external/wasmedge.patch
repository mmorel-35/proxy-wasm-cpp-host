--- a/cmake/Helper.cmake
+++ b/cmake/Helper.cmake
@@ -125,9 +125,8 @@ function(wasmedge_setup_spdlog)
   else()
     include(FetchContent)
     message(STATUS "Downloading fmt source")
+    # Use pre-fetched fmt source from Bazel external dependencies
     FetchContent_Declare(
       fmt
-      GIT_REPOSITORY https://github.com/fmtlib/fmt.git
-      GIT_TAG        11.0.2
-      GIT_SHALLOW    TRUE
+      SOURCE_DIR "$ENV{EXT_BUILD_ROOT}/external/com_github_fmtlib_fmt"
     )
     set(FMT_INSTALL OFF CACHE BOOL "Generate the install target." FORCE)
@@ -153,9 +152,8 @@ function(wasmedge_setup_spdlog)
 
     message(STATUS "Downloading spdlog source")
+    # Use pre-fetched spdlog source from Bazel external dependencies
     FetchContent_Declare(
       spdlog
-      GIT_REPOSITORY https://github.com/gabime/spdlog.git
-      GIT_TAG        v1.13.0
-      GIT_SHALLOW    TRUE
+      SOURCE_DIR "$ENV{EXT_BUILD_ROOT}/external/com_github_gabime_spdlog"
     )
     set(SPDLOG_BUILD_SHARED OFF CACHE BOOL "Build shared library" FORCE)
